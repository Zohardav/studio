
# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Optimized for moderate traffic and cold start balance
  concurrency: 80
  memory: 512Mi
  maxInstances: 10
  minInstances: 0

env:
  - variable: NODE_ENV
    value: production
